# ---- Secrets & credentials (MANDATORY - Appendix F rule 40, guidelines sec. 9) ----
.env
.env.*
!.env.example
credentials.json
token.json
token_*.json
client_secret*.json
service_account*.json
*.pem
*.key
# Tunnel credentials (ngrok / localtonet)
ngrok.yml
ngrok.exe
localtonet*
*.tunnel

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version

# ---- uv ----
# uv.lock IS committed; do not ignore it.

# ---- Testing / coverage ----
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ---- Runtime artifacts / logs (may contain sensitive runtime data) ----
logs/
*.log
runtime/
.local/

# NOTE: signed submission JSON artifacts (declaration_*/config_*/log_*/result_*)
# are produced under artifacts/ and are committed deliberately per the course;
# keep them OUT of logs/ so they are not ignored.

# ---- OS / editor ----
.DS_Store
Thumbs.db
.idea/
*.swp

# ---- Course reference material (never committed to a runtime repo) ----
course_materials/
